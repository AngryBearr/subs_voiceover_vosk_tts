Нужно чтоб ты построил полный пайплайн для синтеза речи на основе того кода что уже есть в проекте. Вот что нужно сделать:
1. SRT => JSON. utils\srt_to_json.py
2. JSON анализ текста для получения полей анализа текста. utils\text_analysis.py
3. JSON нормализация текста для синтеза речи (RUAcent + RUNorm). utils\text_normalization.py
4. Синтез речи на основе нормализованного текста. synthesize\synthesize.py
    - Нужно добавить в синтез условия которые нужно взять из анализа текста - скорость речи минимальное значение между min(базовая скорость равная 1.25, mismatch_ratio - до 2 знаков после запятой, extended_mismatch_ratio - до 2 знаков после запятой)
5. Добавить утилиту по уменьшению пауз в аудио файлах после синтеза. utils\reduce_silence.py
    - Уменьшение пауз больше 0.5 секунд до 0.2 секунд.
    - Возможность регулировки порога и целевой длины пауз через аргументы командной строки.
6. Анализ аудио файлов после синтеза для увеличения скорости речи если длина аудио файла больше чем длина из анализа текста. utils\audio_analysis.py
    - Если длина аудио файла больше чем длина из анализа текста на 10% и более, увеличить скорость речи на 10% и пересинтезировать (или ускорить другими средствами, смотря что быстрее).
7. Сделать resample всех аудио файлов к 44.1 kHz после всех операций. utils\audio_resample.py

Для каждого шага нужно сделать функцию которая принимает на вход необходимые данные и возвращает результат, а также сделать главный скрипт pipeline.py который будет вызывать все эти шаги по порядку, принимая на вход SRT файл и выдавая на выход готовые аудио файлы. Так же было бы не плохо иметь один конфигурационный файл config.json для задания параметров по умолчанию для всех шагов пайплайна.
